import { all, takeLatest, fork } from 'redux-saga/effects';

// import { Types as AuthTypes } from '../reducers/auth';
// import { Types as ClubTypes } from '../reducers/clubs';
// import { Types as GamesTypes } from '../reducers/games';

// import { getUserToken, createNewUser } from './auth';
// import { getAllClubs, createClub, makeTransfer, updatedItemSaga, deleteClubRequest } from './clubs';
// import { createGameRequest } from './game';

export default function* rootSaga() {
  return yield all([]);
}
